{
  "output_file": "settings.json",
  "name": {
    "type": "textbox",
    "value": "pennies",
    "label": "Currency name",
    "tooltip": "The currency name.",
    "group": "Core"
  },
  "frequency": {
    "type": "slider",
    "label": "Interval between currency gains",
    "value": 10,
    "min": 1,
    "max": 60,
    "ticks": 1,
    "tooltip": "This will give users currency at the specified interval.",
    "group": "Core"
  },
  "quantity": {
    "type": "textbox",
    "value": "10",
    "label": "Currency gain",
    "tooltip": "How many points the users will earn at each interval.",
    "group": "Core"
  },
  "only_subs": {
    "type": "checkbox",
    "value": false,
    "label": "Only subs will gain currency",
    "tooltip": "If turned on, only subs will gain currency at each interval.",
    "group": "Core"
  },
  "gain_offline": {
    "type": "checkbox",
    "value": false,
    "label": "Gain currency while offline",
    "tooltip": "If turned on, users on the channel will gain currency while the stream is offline.",
    "group": "Core"
  },
  "exclude_users": {
    "type": "textbox",
    "value": "",
    "label": "Exclude users (separate with ,)",
    "tooltip": "Put usernames that aren't going to gain currency. Each username separated by a comma.",
    "group": "Core"
  },
  "msg_whisper": {
    "type": "checkbox",
    "value": true,
    "label": "Whisper answers",
    "tooltip": "If turned on, bot will reply this currency commands only with whispers.",
    "group": "Messages"
  },
  "msg_count": {
    "type": "textbox",
    "value": "$(user), you currently have $(points) $(currency).",
    "label": "How many currency the user has",
    "tooltip": "Displays how many currency the user has.\n$(user): user's name\n$(points): how many\n$(currency): currency's name",
    "group": "Messages"
  },
  "msg_decrement": {
    "type": "textbox",
    "value": "Removed $(points) $(currency) from $(user). New amount: $(current).",
    "label": "How many currency the user has",
    "tooltip": "Displays new amount after some currency is removed.\n$(user): user's name\n$(points): how many removed\n$(currency): currency's name\n$(current): currency amount after remove",
    "group": "Messages"
  },
  "export_and_view": {
    "type": "button",
    "label": "Export and View Current",
    "tooltip": "Exports users' currency to a txt and view it.",
    "function": "export_and_view_currency",
    "wsevent": "",
    "group": "Utilities"
  }
}